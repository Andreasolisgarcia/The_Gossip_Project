<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

<style>
  .center {
    display: flex;
    justify-content: center;
    height: 100vh;
  }
  form {
    width: 400px;
  }
  .form-header {
    display: flex;
    justify-content: center;
    margin-bottom: 1rem;
  }
  
</style>
<div class="center">
  <%= form_for @gossip do |f| %>
    <div class="form-header">
      <h1>Edit some Gossip info</h1>
    </div>
    <div class="form-group">
      <%= label_tag :title, "Title" %>
      <%= f.text_field :title, class: "form-control",  minlength: 3, maxlength: 14 %>
      <small id="titleHelp" class="form-text text-muted">New title for your gossip</small>
    </div>
    <div class="form-group">
      <%= label_tag :context, "Gossip Content" %>
      <%= f.text_area :context, class: "form-control", required: true, rows: 3 %>
      <small id="contextHelp" class="form-text text-muted">Add or change some Gossip's details.</small>
    </div>
    <div class="form-group form-check">
      <%= check_box_tag :terms_and_conditions, "1", false, class: "form-check-input", required: true%>
      <%= label_tag :terms_and_conditions, "OK, it's ready ", class: "form-check-label" %>
    </div>
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= submit_tag "Change Details", class: "btn btn-primary" %>
  <% end %>
</div>

</body>
</html>