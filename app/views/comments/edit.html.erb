<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

<style>
  .center {
    display: flex;
    justify-content: center;
    height: 100vh;
  }
  form {
    width: 400px;
  }
  .form-header {
    display: flex;
    justify-content: center;
    margin-bottom: 1rem;
  }
  
</style>
<div class="center">
<%= form_for [@gossip, @comment], url: gossip_comment_path(@gossip, @comment), method: :patch do |f| %>
    <div class="form-header">
    <h1>Edit Comment :</h1>
  </div>
    <div class="form-group">
    <%= label_tag :content, "Comment Content" %>
    <%= f.text_area :content, class: "form-control", required: true, rows: 3 %>
    <small id="contextHelp" class="form-text text-muted">Add or change some Comment's details.</small>
    </div>
    <div class="form-group form-check">
      <%= check_box_tag :terms_and_conditions, "1", false, class: "form-check-input", required: true%>
      <%= label_tag :terms_and_conditions, "OK, it's ready ", class: "form-check-label" %>
    </div>
    <div class="form-group">
      <%= link_to "Change my mind", @gossip, class: "btn btn-secondary mt-4 me-2" %>
      <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
      <%= submit_tag "Change Details", class: "btn btn-primary" %>
    <% end %>
  </div>

</body>
</html>