<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User</title>
</head>
<body>

<% if flash[:success].present? %>
  <div class="alert alert-success">
    <%= flash[:success] %>
  </div>
<% end %>


<div class="container">
  <h2 class="text-center mt-5 mb-5">Author :</h2>
  <div class="row">
    <div class="col-md-4 offset-md-4">
      <dl class="dl-horizontal">
        <dt>First Name:</dt>
        <dd><%= @user.first_name %></dd>
        <dt>Last Name:</dt>
        <dd><%= @user.last_name %></dd>
        <dt>Description:</dt>
        <dd><%= @user.description %></dd>
        <dt>Email:</dt>
        <dd><%= @user.email %></dd>
        <dt>Age:</dt>
        <dd><%= @user.age %></dd>
        <dt>City:</dt>
        <dd><%= @user.city.name %></dd>
        <button type="button" class="btn btn-light">
        <%= link_to "Gossip", gossip_path(@user), class: "btn btn-light" %>
        <button type="button" class="btn btn-light">
        <%= link_to "City's Gossips", city_path(@user), class: "btn btn-light" %>
      </dl>
    </div>
  </div>
</div>


</body>
</html>